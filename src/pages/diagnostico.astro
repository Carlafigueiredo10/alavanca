---
// Passo 0 unificado a partir de 2026-05-11: o antigo Diagnóstico de 5 perguntas
// foi substituído pelo módulo Mapear (Wizard de 4 telas + Relatório SEI-Ready).
// Mantemos a URL /diagnostico viva como redirect 301 permanente para preservar
// links externos, bookmarks e referências em peças anteriores.
//
// O sistema de 20 hooks (src/lib/diagnostico/hooks.ts + src/prompts/hooks/*.md)
// permanece funcional internamente — só perde a porta de entrada visual. Pode
// ser ressuscitado depois como caminho secundário se necessário.

export const prerender = false;

return Astro.redirect('/mapear/sprint', 301);
---
